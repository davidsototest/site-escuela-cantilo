
import React from 'react';
import EmailsLayout from '../layout/EmailsLayout';

interface Props {
    // Define props here
}

const EmailsPage: React.FC<Props> = (Props) => {
    return (
        <EmailsLayout/>
    );
};

export default EmailsPage;