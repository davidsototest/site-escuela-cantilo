
import React from 'react';

interface Props {
    // Define props here
}

const EmailsComponent: React.FC<Props> = (Props) => {
    return (
        <div>
            <h1> EmailsComponent </h1>
        </div>
    );
};

export default EmailsComponent;